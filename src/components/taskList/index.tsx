import './style.scss';

const TaskList = (array: any) => {
    return (
        <ul className="main__task-list">
            {/* {
                array.map((item: any) => (
                    <p>{item}</p>
                ))
            } */}
        </ul>
    );
  }
  
  export default TaskList;